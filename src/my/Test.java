package my;

public class Test {

    public static void main(String[] args) {
        // int n = 5;
        // BitSet b1 = new BitSet(n);
        // b1.stream().count();

        // Deque<Integer> q = new LinkedList<>();
        // q = new ArrayDeque<>();

        // Map<Integer, List<Integer>> m = Arrays.asList(1, 2, 3, 3)
        // .stream().map(v -> v).collect(Collectors.groupingBy(v -> v));

        // 特例 nextInt nextLine=为换行符 nextLine
        // Scanner scan = new Scanner(System.in);
        // scan.skip("\r\n");
        // int i = scan.nextInt();
        // String s=scan.nextLine();
    }
}
